# dsh-desktop-windows-launcher bundle patch: mount the plugin under its
# package name, resolved through the profile's node_modules when listed in
# dsh.profile.bundles (dsh plugin add dsh-desktop-windows-launcher).
- insert:
    - id: dsh-desktop-windows-launcher
      name: dsh-desktop-windows-launcher
